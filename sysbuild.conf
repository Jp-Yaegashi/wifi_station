# sysbuild configuration for nRF7002dk WiFi support
# Based on Nordic's official documentation and samples
SB_CONFIG_WIFI_NRF70=y

# Disable bootloader to prevent duplicate boot messages
SB_CONFIG_BOOTLOADER_MCUBOOT=n
